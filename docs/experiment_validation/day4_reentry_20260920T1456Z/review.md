# Day 4 재개 — 2026-09-20 14:56 UTC 입력 변화 확인

**DAY_4_BLOCKED.** 새 PIT 자료가 없어 S6 재현과 S1 공통 표본 비교를 완료하지 못했다.
완료 표식은 기록하지 않는다. 프로토콜의 후보·기준선·선택 게이트와 기존 판단은 그대로다.

직전 10:47 UTC 목록과 같은 로컬 검색 범위의 데이터 파일 3,470개를 대조했다.
추가·삭제 파일은 0개이며, 크기/mtime/ctime 중 차이가 있는 경로는 20개다.
17개 후보 ETF 및 AAPL/SPY/VIX의 일봉 캐시이고, 모두 가격·거래량·날짜 열만 있다.
구성종목·과거 섹터·주식수 공개시각·증권 단위·기업행사/상장폐지 자료의 누락을
해결하지 않으므로 새 전략 입력으로 편입하지 않았다. 검색하지 않은 경로나 외부 자료의
부재를 주장하지 않는다. 기존 보유 PIT 자료의 경로를 요청했으나 이번 확인 중 전달되지 않았다.

로컬 수정시각 차이는 내용 변경이나 과거 공개시각을 뜻하지 않는다. 이전 내용 해시를
대조할 수 있는 AAPL/SPY/VIX 3개는 모두 그대로다. 나머지 17개에 대해 내용이 바뀌었다고
단정하지 않는다. 전체 목록은 `local_scan.csv.gz`, 범위·비교 기준·20개 스키마/해시는
`local_input_delta.json`에 보존했다. 이 파일들은 시장 데이터의 PIT 인증서가 아니다.

## 무결성 및 누수·체결 검증 범위

- 기존 Day 4 원천 1,548개와 동결 전략 데이터 22개의 내용 해시가 일치한다.
- 앞선 증거 목록 268+20+18+16+7개 항목의 해시가 모두 일치한다. 원본 spec/manifest/lock,
  후보 YAML, 기존 판단과 누수/체결 감사 코드도 보존됐다.
- 저장 비교 상태 가드 PASS: 인증 반기/공통 거래일 0개, 실제/프록시 × 5/10/25bp 비교
  6개 전부 NOT_EVALUABLE이고 수익률을 0으로 채우지 않았다. 완료 표식 부재도 확인했다.
- 코드가 바뀌지 않아 이미 통과한 pytest와 결정론적 백테스트는 다시 실행하지 않았다.
  직전 15 passed 로그의 해시를 검증했으며 이는 **이전 테스트 결과**다. 이번 새 검사는
  메타데이터 차이·가격 캐시 스키마·저장 상태·해시 대조다 (`checks.log`, `validation.json`).

현재 섹터를 과거에 적용하거나 최초 미래 주식수/구성종목으로 결측을 채우는 경로,
체결일 종가를 같은 날 시가 선정에 사용하는 경로, 종가 수익률 shift를 시가 체결로
간주하는 경로는 기존 감사에서 확인된 누수/체결 문제다. 이번에는 해당 경로를 실행하지
않았다. S6 주문이 없으므로 위반 0건을 S6 실행 검증 통과로 주장하지 않는다.
기존 S1 저장 주문 감사 결과도 S6 검증을 대신하지 않는다.

동결 데이터 목록 SHA-256:
`a8132c4441f818e47309dee4cbb93bb3067b18edc40371bda00679862fb6e86d`.
기존 원천 목록 SHA-256:
`5b1b140be1a06dcbff5829bd82430fd5123baf8083ef4c5df6bebf4cd1e77123`.
개별 생성 파일 해시는 `result_hashes.json`에 기록한다.

새 사전등록 해석이나 코드 수정은 없어서 새 판단 체크포인트는 필요하지 않았다.
기존 미커밋 파일은 보존하고 진행/재개 문서에는 이번 결과만 덧붙인다.
게시 브랜치는 `research/day4-reentry-20260920T1456Z`이며 원격 확인·Telegram 결과는
`publication.json`에 기록한다. 일일 HTML은 게시 대상이 아니다.

다음 자동 감독은 Day 4의 실제 PIT 입력부터 확보한다. 입력 계약은
`../day4_input_handoff_20260920/input_request.md`, 구체적인 재개 명령은 이 폴더의
`RESUME_NOTE.md`를 따른다. 새 증거가 없으면 같은 백테스트·원천 조사·요청 패키지를
반복하지 않는다. Day 5는 시작하지 않았다.
