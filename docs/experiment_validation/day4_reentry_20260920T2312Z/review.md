# Day 4 재개 — 2026-09-20 23:12 UTC

**DAY_4_BLOCKED.** 직전 19:05 UTC 이후 새 PIT 입력이 없어 S6 재현·S1 공통 표본
비교를 완료하지 못했다. 인증 반기/공통 거래일 0개, 실제·프록시 × 5/10/25bp의
비교 6개는 NOT_EVALUABLE이다. 완료 표식을 쓰지 않으며 Day 5는 시작하지 않았다.

동일한 로컬 탐색 범위의 데이터 파일 3,482개를 직전 목록과 대조했다. 추가·삭제·
크기/mtime/ctime 변경은 모두 0개다. 실험 디렉터리에서 직전 목록 이후 기록된 파일도
이전 실행의 증거·게시 영수증과 진행/재개 문서뿐이었다. 목록과 비교 시각·범위는
`local_scan.csv.gz`, `local_input_delta.json`에 보존했다. 검색하지 않은 경로나
외부 공급자에 자료가 없다고 주장하지 않는다. 별도 자료 경로는 전달받지 못했다.

기존 원천 1,548개, 동결 데이터 22개, 이전 증거 목록의 268+20+18+16+7+7+8개
항목 해시는 일치했다. 새 목록의 독립 대조, 기존 비교의 빈 성과 값 및 완료 표식
가드도 PASS다. `validation.json`, `checks.log`에 범위를 기록했다. 이전 15 passed
로그와 감사 코드 해시를 확인했으며 코드·입력이 그대로여서 pytest와 백테스트를
반복하지 않았다. 이 검사는 실제 S6의 누수 없음이나 다음 시가 체결을 인증하지 않는다.

기존 `../satellite_audit.md`에서 확인한 현재 섹터의 과거 적용, 미래 주식수/구성종목
fallback, 시가 선정에 체결일 종가를 섞는 경로, 종가 수익률 shift의 체결 근사는
여전히 실제 재현에 사용할 수 없다. 그 경로로 S6를 실행하거나 미인증 날짜를 현금
성과로 채우지 않았다. 후보·기준선·게이트·사전등록 해석 변경이나 코드 결함 수정은
없으므로 새 판단 체크포인트도 만들지 않았다.

동결 데이터 목록 SHA-256:
`a8132c4441f818e47309dee4cbb93bb3067b18edc40371bda00679862fb6e86d`.
기존 원천 목록 SHA-256:
`5b1b140be1a06dcbff5829bd82430fd5123baf8083ef4c5df6bebf4cd1e77123`.
이번 파일별 해시는 `result_hashes.json`을 참조한다.

다음 자동 감독은 실제 PIT export/기존 자료 경로를 확보한 뒤 입력 인증부터 진행한다.
필요 필드는 `../day4_input_handoff_20260920/input_request.md`에 이미 정리되어 있다.
40개 반기를 모두 인증해야 한다는 추가 조건은 없으며, 실제 인증 가능한 기간만
평가한다. 새 증거 없이 같은 원천 조사·요청 생성·백테스트를 반복하지 않는다.
자세한 재개 명령은 `RESUME_NOTE.md`에 있다.

기존 미커밋 파일과 main index는 보존하고 진행/재개 문서에는 이번 기록만 추가한다.
연구 브랜치는 `research/day4-reentry-20260920T2312Z`이며 게시·Telegram 결과는
`publication.json`에 별도로 기록한다. 자동 생성 일일 HTML은 커밋하지 않는다.
