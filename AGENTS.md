# 작업 인계 규칙

- 세션 시작 시 먼저 `docs/SESSION_HANDOFF.md`와 루트 `PROGRESS.md`를 읽는다.
- 관련 로드맵·스펙·검증 문서를 확인한 뒤 작업한다.
- 작업 종료 시 결정, 근거, 수정 파일, 검증 결과, 다음 단계를 인계 문서에 갱신한다.
- 제안·구현·검증·배포 상태를 구분해 기록한다. 제안을 실행 완료로 표시하지 않는다.
- API 키·토큰·개인정보·민감한 운영값은 문서에 기록하지 않는다.
- 모든 대화 원문을 복사하지 말고, 재개에 필요한 결정 중심 요약만 남긴다.
- 이 규칙은 저장소를 이용하는 에이전트의 작업 절차이며, 모든 대화 원문이 자동 저장된다는 뜻은 아니다.
- 사용자 요청 범위(명시적 요청과 맥락상 위임 포함)를 기준으로 진행한다.
- 모델·에이전트 배분은 특정 요청의 작업 분담일 수 있으나 자동으로 강제하지 않는다.

# 관제 센터 사용 설명서 유지 규칙

사용자는 Quant VM 관제 센터의 `/guide`(사용 설명서)로 대시보드와 엔진을 쓴다. 엔진이 바뀌면 설명서도 함께 바뀌어야 한다.

- 화면(`core/app_navigation.py`), 자동 잡(`core/job_schedule.py`·`core/process_registry.py`), `core/*.py` 모듈, `scripts/*.py`를 추가·삭제·이름 변경할 때 `hub/guide/content_*.py`를 같은 변경에서 고친다.
- 화면 목록, 자동 잡 표(시각·켜짐/꺼짐), 최근 변경, 배포 버전은 코드에서 자동으로 읽으므로 손으로 적지 않는다. 사람이 쓰는 것은 "무엇이고 어떻게 쓰나"다.
- 빠뜨리면 `tests/test_hub_guide.py`가 실패하고 자동배포의 테스트 관문이 배포를 막는다. 무엇이 빠졌는지는 `python -m hub.guide.check`로 본다.
- 사용자에게 설명할 필요가 없는 내부 유틸은 `INTERNAL` 목록에 한 줄 이유와 함께 적는다. 애매하면 설명을 쓰는 쪽을 택한다.
- 기존 동작을 바꿨다면 해당 항목을 코드와 다시 대조하고 `verified` 날짜를 갱신한다. 근거 파일이 `verified` 이후에 커밋되면 화면에 "코드가 변경됨" 경고가 뜬다.
- 설명에는 키·비밀번호·토큰·이메일 값을 적지 않고, 성과·승률 개선을 주장하지 않는다.
