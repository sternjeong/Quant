"""엔진 모듈 설명 — core/ 의 앞쪽 절반(파일명 a~l). (내용은 채워 넣는 중)"""

from hub.guide.schema import ModuleGuide

MODULES: tuple[ModuleGuide, ...] = ()
INTERNAL: dict = {}  # 사용자에게 설명할 필요 없는 내부 유틸: {"core/x.py": "이유"}
